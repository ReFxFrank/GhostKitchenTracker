export default function MethodologyPage() {
  return (
    <div className="max-w-3xl mx-auto px-6 py-24">
      <h1 className="text-2xl font-semibold tracking-tight">Methodology</h1>
      <p className="mt-4 text-neutral-600">
        The full public methodology — evidence families, weights, tiers, and
        the rules that bound what this site will and will not assert — is
        maintained in the project repository as{" "}
        <code className="text-sm">docs/METHODOLOGY.md</code> and will render
        here when the site goes live (Phase 4).
      </p>
    </div>
  );
}
